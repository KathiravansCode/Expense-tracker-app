# Integration-test profile. Datasource comes from Testcontainers via @DynamicPropertySource.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

jwt.secret=test-secret
jwt.expiration=86400000
